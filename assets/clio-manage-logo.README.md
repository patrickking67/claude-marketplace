# Clio Manage logo

Drop the **official Clio Manage logo** here as `clio-manage-logo.png` (obtain from Clio's brand/press kit and use per Clio's trademark/brand guidelines — do not alter it).

It is referenced by the planned remote **clio-manage** connector (`connectors/mcp-servers.example.json` → `icon`) so the connector listing shows the Clio Manage mark. There is no logo field in the plugin/marketplace schema itself, so the logo applies to the connector, not the marketplace tile.
